# Copy to .env.local and edit as needed.

# Set to 'true' to use mock data (no Flask backend required).
# Set to 'false' to call the Flask API at NEXT_PUBLIC_API_URL.
NEXT_PUBLIC_USE_MOCK=true

# Flask backend base URL (must NOT end with trailing slash).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8080
